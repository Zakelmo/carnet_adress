# Configuration de l'application Cabinet Médical
# Copiez ce fichier en .env et remplissez vos valeurs

# Flask Configuration
SECRET_KEY=votre-cle-secrete-aleatoire

# Database Configuration
DATABASE_NAME=contacts.db

# ============================================
# Configuration Email (SMTP)
# ============================================
# Pour Gmail:
#   SMTP_SERVER=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USERNAME=votre-email@gmail.com
#   SMTP_PASSWORD=votre-mot-de-passe-d-application  (pas votre mot de passe Gmail!)
#   SMTP_USE_TLS=true
#
# Pour Outlook/Hotmail:
#   SMTP_SERVER=smtp.office365.com
#   SMTP_PORT=587
#   SMTP_USERNAME=votre-email@outlook.com
#   SMTP_PASSWORD=votre-mot-de-passe
#   SMTP_USE_TLS=true
#
# Pour un serveur SMTP personnalisé:
#   SMTP_SERVER=smtp.votre-domaine.com
#   SMTP_PORT=587 (ou 465 pour SSL)
#   SMTP_USERNAME=votre-email@domaine.com
#   SMTP_PASSWORD=votre-mot-de-passe
#   SMTP_USE_TLS=true (false pour SSL sur port 465)

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
DEFAULT_SENDER_EMAIL=
DEFAULT_SENDER_NAME=Cabinet Médical

# ============================================
# Configuration WhatsApp (Twilio)
# ============================================
# 1. Créez un compte sur https://www.twilio.com
# 2. Obtenez votre ACCOUNT_SID et AUTH_TOKEN
# 3. Activez WhatsApp Sandbox et obtenez votre numéro
# 4. Les utilisateurs doivent rejoindre le sandbox en envoyant le message de jointure

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# ============================================
# Limites d'envoi (protection anti-spam)
# ============================================
MAX_EMAILS_PER_HOUR=50
MAX_WHATSAPP_PER_HOUR=30
